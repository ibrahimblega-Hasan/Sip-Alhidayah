# Database (SQLite for local dev, swap to PostgreSQL connection string in production e.g. Vercel Postgres / Supabase / Neon)
DATABASE_URL="file:./dev.db"

# Auth (demo credentials - replace with a real auth provider such as NextAuth/Auth.js or Clerk in production)
NEXTAUTH_SECRET="replace-with-a-random-32-char-secret"
NEXTAUTH_URL="http://localhost:3000"

# App
NEXT_PUBLIC_APP_NAME="Sistem Informasi Pondok"
NEXT_PUBLIC_SCHOOL_NAME="Pondok Pesantren Al-Hikmah"
